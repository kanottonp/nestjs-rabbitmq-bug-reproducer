import { Test, TestingModule } from '@nestjs/testing';
import { Inventory1Controller } from './inventory-1.controller';
import { Inventory1Service } from './inventory-1.service';

describe('Inventory1Controller', () => {
  // let inventory1Controller: Inventory1Controller;

  // beforeEach(async () => {
  //   const app: TestingModule = await Test.createTestingModule({
  //     controllers: [Inventory1Controller],
  //     providers: [Inventory1Service],
  //   }).compile();

  //   inventory1Controller = app.get<Inventory1Controller>(Inventory1Controller);
  // });
});
