import { Test, TestingModule } from '@nestjs/testing';
import { Inventory2Controller } from './inventory-2.controller';
import { Inventory2Service } from './inventory-2.service';

describe('Inventory2Controller', () => {
  // let inventory2Controller: Inventory2Controller;

  // beforeEach(async () => {
  //   const app: TestingModule = await Test.createTestingModule({
  //     controllers: [Inventory2Controller],
  //     providers: [Inventory2Service],
  //   }).compile();

  //   inventory2Controller = app.get<Inventory2Controller>(Inventory2Controller);
  // });

  // describe('root', () => {
  //   it('should return "Hello World!"', () => {
  //     expect(inventory2Controller.getHello()).toBe('Hello World!');
  //   });
  // });
});
